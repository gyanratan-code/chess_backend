{
  "name": "websocket_server",
  "version": "1.0.0",
  "description": "It is a chess webSocket Server to handle seemless communication through webSocket",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node dist/index.js"
  },
  "keywords": [
    "chess",
    "websocket",
    "server"
  ],
  "author": "Gyan Ratan",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "socket.io": "^4.8.1"
  },
  "optionalDependencies": {
    "bufferutil": "^4.0.9",
    "utf-8-validate": "^6.0.5"
  }
}
